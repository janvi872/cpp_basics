#include<iostream>
using namespace std;

int main(){
    int mid, x, len;
    cout<<"Enter the number: "<<endl;
    cin>>x;
    int arr[]={1, 2, 5, 20, 5};
    len = sizeof(arr)/sizeof(int);
    mid = arr[0+len/2];
    while()
    if (arr[i]==mid) {
        return mid;
    } else if(arr[i]<mid) {
        left
    }

    return 0;
}
